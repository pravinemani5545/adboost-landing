---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import CtaBand from '../../components/CtaBand.astro';
import { SERVICES } from '../../data/services';
import { siteBase } from '../../lib/site';
import { breadcrumb } from '../../lib/schema';

const SITE = siteBase(Astro.site);
const title = 'Services: Paid Media, Creative, CRO & Compliance | AdBoost Health';
const description = 'Compliant paid media, in-house creative, CRO, and pre-launch compliance review — the full acquisition system for health, telehealth, GLP-1 and supplement brands.';
const jsonLd = [
  breadcrumb(SITE, [{ label: 'Home', href: '/' }, { label: 'Services', href: '/services/' }]),
  { '@type': 'CollectionPage', name: 'AdBoost Health services', url: `${SITE}/services/` },
];
---
<BaseLayout {title} {description} canonicalPath="/services/" {jsonLd}>
  <section class="phero wide">
    <div class="container">
      <Breadcrumbs items={[{ label: 'Home', href: '/' }, { label: 'Services' }]} />
      <span class="eyebrow sec-eyebrow" style="display:block;margin-top:16px"><span class="dot"></span>SERVICES</span>
      <h1>One <span class="serif">compliant</span> acquisition system, built for health.</h1>
      <p class="phero-lede">Paid media, creative, and conversion — with compliance review wrapped around all of it. The four services below work as one engine: the reason our partners average 24% lower CAC in the first 30 days.</p>
    </div>
  </section>

  <section class="iwrap">
    <div class="container">
      <div class="grid-cards four">
        {SERVICES.map((s) => (
          <a class="lcard reveal" href={`/services/${s.slug}/`}>
            <h3>{s.name}</h3>
            <p>{s.lede}</p>
            <span class="lc-more">Learn more →</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <CtaBand />
</BaseLayout>
